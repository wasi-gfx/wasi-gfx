# wasi-gfx:frame-buffer

WIT interface for raw pixel buffer access.

Provides a simple `device` and `buffer` abstraction for reading and writing raw pixel data. Useful for software renderers, image processing, or any case where you want direct pixel control without a GPU API.
